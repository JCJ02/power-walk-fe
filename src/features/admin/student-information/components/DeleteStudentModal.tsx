import { useEffect } from "react";

const DeleteStudentModal = () => {
  useEffect(() => {
    document.title = "Delete Student - Power Walk Technology";
  });
  return <>Delete Student Modal</>;
};

export default DeleteStudentModal;
